# RSS 优选下载插件仓库

这是一个用于 `MoviePilot V2` 的第三方插件仓库，当前包含两个插件：

- `RSS优选下载`
- `媒体库缺集补全`

## 功能

### 媒体库缺集补全

- 不看订阅列表，直接扫描你配置的媒体服务器实际媒体库
- 只处理电视剧，按 `TMDB` 识别媒体、季和已播出剧集
- 计算媒体库里每一季实际缺失的剧集后，直接调用 `MoviePilot` 搜索和下载链路
- 可指定扫描哪些媒体服务器，也可指定搜索站点
- 默认带 `24` 小时短期去重冷却，避免媒体库还没刷新时重复推送同一批缺集
- 可选择只补“已播出剧集”，避免把未来还没播出的集数也算成缺失
- 可按标题正则做包含/排除筛选

### RSS优选下载

- 读取 PT 站 RSS 条目
- 使用 MoviePilot 自身识别能力识别 `TMDB / 季 / 集`
- 同一集同时出现多个版本时，优先下载更高质量版本
- 默认优先级：`2160p/4K > 1080p > 720p > 其他`
- 支持配置不同站点优先级，在同清晰度下优先选择指定站点
- 同分辨率下可优先 `HEVC/H.265`
- 如果后续刷新又出现同一集，并且新资源体积更大，会再次推送下载
- 整季/完结包会先检查媒体库：只有该季已经完整入库才跳过；如果只是部分集数，仍允许整季包补齐
- `E01-E02`、`S01E01-E04`、`第1-2集` 这类多集连发默认保留，不会被当成完结包过滤
- 历史记录会优先显示到“集”，例如 `S01E01`、`S01E01-E02`
- 默认跳过“电视剧无集号”的条目，例如只有 `S01`、没有 `E01` 的资源，避免破坏按集优选
- 现在会额外从标题附加信息里提取中文集号，例如 `第39-40集`、`第09-10集`

## 仓库结构

```text
package.v2.json
plugins.v2/
  librarygapfill/
    __init__.py
  rssbestversion/
    __init__.py
```

## 站点优先级配置

在插件配置页的“站点优先级”里，每行填一个：

```text
m-team.cc=100
pt.example.com=80
hdhome.org=60
```

规则说明：

- 分值越大优先级越高
- 未配置的站点默认是 `0`
- 支持直接写域名，也支持子域名匹配
- 站点优先级在排序中位于“清晰度”之后，“编码/体积”之前

## 当前版本

- `LibraryGapFill` `v1.0`
- `RssBestVersion` `v1.8`
